---
title: Lock Ratio
---

# Lock Ratio

Set `lock` to preserve the current aspect ratio while resizing from both middle and corner handles.

```tsx
<ReactDragResizeRotate w={180} h={120} x={80} y={80} lock />
```
