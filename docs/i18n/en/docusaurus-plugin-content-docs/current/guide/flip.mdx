---
title: Flip
---

# Flip

When no minimum size is set, dragging a resize handle across the opposite edge triggers a flip and calls `onFliped`.

```tsx
<ReactDragResizeRotate onFliped={(pos) => console.log(pos.flipSign)} />
```
