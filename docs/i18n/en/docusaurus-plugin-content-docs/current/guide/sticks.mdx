---
title: Sticks
---

# Sticks

Use `sticks` to choose the visible resize and rotate handles. Pass `[]` to hide all controls.

```tsx
<ReactDragResizeRotate sticks={['tl', 'tr', 'br', 'bl', 'angle']} />
```
