---
title: Events
---

# Events

All event callbacks use `(pos: PosData, event: MouseEvent) => void`.

Callbacks: `onActivated`, `onDragStart`, `onDragging`, `onDragStop`, `onResizeStart`, `onResizing`, `onResizeStop`, `onRotateStart`, `onRotating`, `onRotateStop`, and `onFliped`.
