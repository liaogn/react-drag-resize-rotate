---
title: 尺寸限制
---

# 尺寸限制

通过 `minWidth`、`minHeight`、`maxWidth`、`maxHeight` 限制缩放尺寸。设置最小尺寸后，缩放越过对边不会触发翻转。

```tsx
<ReactDragResizeRotate minWidth={80} minHeight={80} maxWidth={240} maxHeight={200} />
```
